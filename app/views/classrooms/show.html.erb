<div class="col-6">
<h2><%= @classroom.subject_name %> Students</h2>
<br>
<%= form_with model: [ @classroom, @classroom.students.build ] do |form| %>
    <p>
      <%= form.label :name %><br>
      <%= form.text_field :name,  class: "form-control", placeholder: "Add Student" %>
    </p>
    <p>
      <%= form.submit %>
    </p>
  <% end %>
<br>
  <table class="table table-hover">
    <tr>  
      <th>Name</th>
      <th>Actions</th>
    </tr>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.name %></td>
        <td><%= link_to "Edit Student", "/classrooms/#{@classroom.id}/students/#{student.id}/edit" %></td>
        <td><%= link_to "Delete Student", "/classrooms/#{@classroom.id}/students/#{student.id}", method: :delete %></td>
      </tr>
    <% end %>
  </table>
</div>